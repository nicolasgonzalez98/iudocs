<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Email de la admin principal
    |--------------------------------------------------------------------------
    | Al registrarse (o loguear con Google) con este email, el usuario queda
    | automáticamente como admin + activo. Se define en .env (ADMIN_EMAIL).
    */
    'admin_email' => env('ADMIN_EMAIL'),
];
